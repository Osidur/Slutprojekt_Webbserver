app.post('/forum', async function (req, res) {
            if (true) {
                await bcrypt.compare(req.body.userpassword, user.password, (err, success) => {
                        if (true) {}
                        if (true) {} 
                        else {} 
                        else {
                            console.log("this is brokey")
                            res.render("a bad erroor")
                        }
                    }
                }
            })